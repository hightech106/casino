/**
 * Directory path utility module for resolving project root directory.
 * Exports __dirname for use in path resolution throughout the application.
 * Provides consistent base path for file operations and module imports.
 */
module.exports = {
    dir: __dirname
}