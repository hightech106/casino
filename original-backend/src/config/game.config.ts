/**
 * Game configuration type definitions and constants.
 * Currently empty but serves as a type-safe container for game-specific settings.
 * Extend this to add game configuration schemas as needed.
 */
export const gameConfig = {} as const;

export type GameConfig = typeof gameConfig; 